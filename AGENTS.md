# 项目会话交接

- 开始处理本项目任务前，完整阅读根目录的 `upgrade_handoff.md`，尤其第 0 节，并核对源码、`git status` 和最近提交。该文档是跨会话上下文入口；当前用户要求优先，实际实现以源码为准。
- 每次完成开发或交接任务前更新 `upgrade_handoff.md`，保留必要历史，替换过时描述，准确记录本轮要求、最终实现、计算口径、相关文件、已通过验证、未验证项目、待办和可核实的提交/推送状态。不要把计划写成已完成，不要依赖下一会话能访问之前的聊天。
- 交接文件名固定为小写 `upgrade_handoff.md`，相关引用保持一致。不要将个人账单、备份内容或密钥写入交接文档。
- 保留现有账单与备份兼容性；不要通过清空 IndexedDB、改数据库名或删除旧存储解决升级问题。维持 iPhone 优先、纯本地、无账户/后端/追踪的产品约束。
- 根据本轮用户要求确定范围；交接文档中的已知问题列表不等于全部已获实施授权。提交、推送和发布按当前会话的用户授权执行。
