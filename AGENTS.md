# 项目会话规则

- 新会话快速阅读根目录的 `upgrade_handoff.md` 即可；当前用户要求优先。
- 只查看本轮直接相关的代码，不要求每次扫描整个项目或读取完整 Git 历史。
- 默认不读取历史归档（如 `docs/upgrade_history.md`），仅任务确需追溯时按需读取。
- 只修改用户明确要求的内容；交接中的已知问题不等于已获修复授权。
- 保留 iPhone 优先、纯本地、无账户/后端/追踪，以及旧账单与备份兼容的约束。
- 不通过清空 IndexedDB、改数据库名或删除旧存储解决升级问题。

## 最小必要验证

- 小型 UI、文字、样式、布局、图标、显示逻辑、简单条件判断等低风险修改：完成后直接说明改动。
- 低风险修改默认不运行任何测试、lint、build、CRUD、备份恢复、IndexedDB、Service Worker、PWA、GitHub Pages 或兼容性检查。
- 普通功能只检查直接涉及的代码和逻辑，不默认全项目扫描或全量回归。
- 仅以下高风险改动进行对应验证：IndexedDB/`database.ts`、`backup.ts`、JSON/CSV 格式、数据 schema/分类 ID、迁移、Service Worker/PWA、Vite/Pages/部署配置、依赖升级。
- 高风险也只验证受影响部分；普通图标或文案不因应用使用 PWA 而升级为高风险。
- 不为“保险”额外验证，不因历史记录重复执行；仅用户明确要求“完整验证”“发布前检查”“全面测试”才做较完整验证。
- 提交、push、更新版本号本身不等于完整验证授权；发布依照当前任务的用户授权。

## 交接维护

- 结束前更新 `upgrade_handoff.md`，保持 100～150 行以内，仅保留当前状态、简要技术栈与目录、长期约束、已知问题、最近未发布改动和开发规则。
- 替换过时内容，不累积已发布版本过程、检查结果或 Git/Actions 流水账。
- 未运行验证如实说明即可，不为补写文档增加检查；提交和发布状态不得推断为成功。
- 文件名固定为小写 `upgrade_handoff.md`；不写入个人账单、备份内容、密钥或完整聊天记录。