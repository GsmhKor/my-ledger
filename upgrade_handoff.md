# 我的账单交接 · 1.8.1

最后更新：2026-09-19。新会话读本文即可，按当前需求查看相关代码；开发规则见 `AGENTS.md`。

## 当前状态

`my-ledger` 是 iPhone 优先的个人收支记账 PWA，与 `my-assets` 独立。纯本地，无账户、后端、同步或追踪，不迁移或改写另一应用的数据。

- 当前源码版本 `1.8.1`；版本源为 `package.json`，锁文件仅同步项目根版本，未升级依赖。
- 仓库：<https://github.com/GsmhKor/my-ledger>，分支 `main`；线上路径 `/my-ledger/`。
- 已有月度汇总、记账、账单编辑/删除/筛选、统计、JSON 备份恢复及深色模式。
- 奶油猫咪主题；首页绣球花背景、野餐猫记账按钮，共享猫咪分类图标用于记账、账单、筛选和统计。
- 本轮未提交、推送或部署；线上状态未查询。

## 技术与关键文件

- React 19、TypeScript 6、Vite 8；idb 存储，vite-plugin-pwa 管理安装与缓存，oxlint 静态检查。
- `src/pages/`：首页、账单、统计、设置；`src/App.tsx` / `src/App.css`：导航、月份、弹层、更新入口与样式。
- `src/hooks/useAppUpdate.ts` / `src/services/appUpdate.ts`：更新检查、前台恢复检查、等待下载及手动激活。
- `src/hooks/useLocalDate.ts`：本地零点定时刷新，恢复可见、页面恢复或窗口聚焦时校正日期；首页与统计页共用日期。
- `src/components/`：记账编辑器、账单行、`CategorySymbol` 图标及 `CategoryFilter` 筛选。
- `src/services/database.ts` / `backup.ts`：存储与备份；`src/hooks/useLedger.ts` / `useDarkMode.ts`：账本与主题状态。
- `src/constants/categories.ts`、`src/types/`、`src/utils/`：分类、类型、日期与金额工具。
- `src/assets/`、`public/`：图片；`design/`、`scripts/`：素材来源与重建，改图片时按需读取。
- `vite.config.ts`、`index.html`、`.github/workflows/`：PWA、安装名称、Pages 部署；`tests/appUpdate.test.mjs`：更新流程定向测试。

## 数据与统计约定

- 实际记账仅支持 JPY 正安全整数，收支由 `type` 表示；CNY 仅为类型预留。日期使用本地 `YYYY-MM-DD`。
- 保留旧账单、分类 ID 和备份兼容；禁止通过清空 IndexedDB、改库名、删除存储或卸载来解决升级问题。
- IndexedDB：`my-ledger-db`，版本 `1`，仓库 `transactions`，主键 `id`；主题键 `my-ledger-theme`。
- JSON：`app: 'my-ledger'`、`schemaVersion: 1`；恢复在同一事务中覆盖全部账单，保留全部字段；清空数据须二次确认。
- `retired: true` 只停用新增选项；旧账单保留原分类，历史显示、筛选、统计、导出仍识别。
- 首页本日支出只累计本地今天的支出，不随所选月份变化；统计页不再展示本日、平均支出，保留本月预测、分类及趋势图。
- 当前月日均为截至今天的支出 ÷ 已过天数，包含零支出日；预测为未舍入日均 × 本月天数，显示整数日元。
- 历史月按整月统计，预测为实际总额；未来月日均/预测显示「—」。收入不参与；原空状态、分类与趋势图保持不变。

## 更新行为与最近改动

- 1.8.1：移除 CSV 导出入口及实现，设置页仅保留 JSON 完整备份和恢复；恢复入口文案为“从备份恢复”，JSON 格式、下载及恢复逻辑不变。
- 首页本日支出、平均支出和统计页预测随日期重算；统计页仅保留本月预测与图表，预测算法不变。
- 跨日只更新日期状态，不重载页面、不改账单、不重置编辑表单，也不切换所选月份；今天无支出时显示 0，已有记录时显示实际金额。
- 首页保持上方本日支出、左下本月支出、右下平均支出；布局及最近记录保持不变。
- 桌面新安装默认名称固定为“我的账单”，manifest 与 iPhone 安装标题均不带版本；设置页仍显示版本号。
- 参考 `my-assets` 增加“查看更新”和“更新应用”，无新版、失败、超时均有提示；更新服务就绪前禁用检查按钮。
- 打开应用、恢复前台及恢复联网时检查新版；PWA 使用 `prompt`，用户点击后激活并刷新，无需反复添加桌面图标。
- 编辑弹层、数据读取、保存、删除、备份解析/恢复期间禁用应用更新；更新期间禁止页面交互，其他窗口激活新版不强制刷新本窗口。
- WebP 纳入离线预缓存；安装路径与 Service Worker 路径保持原有配置；本轮未修改数据库、备份格式或分类 ID。
- 旧版尚无更新入口时，联网打开等待下载后关闭应用及同应用网页再重开；无需删除图标或清理本地数据。
- 延续：首页最近 5 笔与编辑入口、默认浅色及已选主题记忆、固定 JSON 备份文件名、打开编辑不主动唤起键盘。

## 已知限制与验证边界

- 最近仅调整恢复入口文案，未运行测试或构建；此前删除 CSV 时已定向核对剩余 JSON 备份代码不变。
- iPhone 真机验证仍待进行；既有桌面图标标题是否自动更新由系统决定。
- 初始读取失败缺少提示/重试；删除、清空异常及并发交互仍需完善；JSON 字段校验、CNY 导入和累计溢出保护仍有缺口。
- iPhone 键盘、安全区、长金额、深色、下载/恢复仍需真机确认；弹层焦点、图表无障碍、maskable 安全区与系统主题跟随待完善。仅按当前任务授权处理。
