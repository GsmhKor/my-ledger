# 我的账单交接 · 1.6.2

最后更新：2026-09-17。新会话读本文即可，按当前需求查看相关代码；开发规则见 `AGENTS.md`。

## 项目边界

`my-ledger` 是 iPhone 优先的个人收支记账 PWA，与 `my-assets` 独立。纯本地，无账户、后端、同步或追踪，不迁移或改写另一应用的数据。

- 已有月度汇总、记账、账单编辑/删除/筛选、统计、JSON 备份恢复、CSV 导出及深色模式。
- 支出新增分类：餐饮、交通、日用品、金丝熊、其他；收入 5 类。停用分类仍用于历史记录。
- 奶油猫咪主题；首页绣球花背景透明度 70%，记账按钮为野餐猫，共享分类图标用于记账、账单、筛选和统计。
- 仓库：<https://github.com/GsmhKor/my-ledger>，分支 `main`；线上路径 `/my-ledger/`。

## 技术与关键文件

- React 19、TypeScript 6、Vite 8；idb 存储，vite-plugin-pwa 管理安装与缓存，oxlint 静态检查。
- `src/pages/`：首页、账单、统计、设置；`src/App.tsx` / `src/App.css`：导航、共享月份、弹层与样式。
- `src/components/`：记账编辑器、账单行、`CategorySymbol` 图标及 `CategoryFilter` 筛选。
- `src/services/database.ts` / `backup.ts`：存储与备份；`src/hooks/`：账本与主题状态。
- `src/constants/categories.ts`、`src/types/`、`src/utils/`：分类、类型、日期与金额工具。
- `src/assets/`、`public/`：图片；`design/`、`scripts/`：素材来源与重建，改图片时按需读取。
- `vite.config.ts`、`index.html`、`.github/workflows/`：PWA、安装名称、Pages 部署；版本源为 `package.json`。

## 数据与统计约定

- 实际记账仅支持 JPY 正安全整数，收支由 `type` 表示；CNY 仅为类型预留。日期使用本地 `YYYY-MM-DD`。
- 保留旧账单、分类 ID 和备份兼容；禁止通过清空 IndexedDB、改库名、删除存储或卸载来解决升级问题。
- IndexedDB：`my-ledger-db`，版本 `1`，仓库 `transactions`，主键 `id`；主题键 `my-ledger-theme`。
- JSON：`app: 'my-ledger'`、`schemaVersion: 1`；恢复在同一事务中覆盖全部账单，保留全部字段；清空数据须二次确认。
- `retired: true` 只停用新增选项；旧账单可保留原分类，历史显示、筛选、统计、导出仍识别。
- 最大支出为所选月最高单日支出，含已录入未来日期；当前月日均为截至今天的支出 ÷ 已过天数，包含零支出日。
- 当前月预测为未舍入日均 × 本月天数，显示整数日元；历史月按整月统计，预测为实际总额；未来月日均/预测显示「—」。收入不参与。

## 已知限制

仅作按需排查线索，不是本轮修复或验证清单。

- WebP 未纳入离线预缓存；PWA 缺少更新提示、恢复前台检查和未保存表单保护。
- 初始读取失败缺少提示/重试；删除、清空异常及并发交互仍需完善。
- JSON 日期、时间戳、ID、分类类型校验不完整；导入 CNY 与 JPY 编辑/汇总不一致；金额校验及累计溢出保护仍有缺口。
- iPhone 键盘、安全区域、长金额、深色、离线、下载/恢复未完整实测；弹层焦点、图表无障碍、CSV 转义、maskable 安全区及系统主题跟随仍有待完善。

## 最近改动与验证状态

- `1.6.2`：首页按用户最新决定保留“最近记录”，展示所选月份最近 5 笔，支持点击编辑及“查看全部”进入账单页；无记录时显示空状态。
- 桌面默认名称为“我的账单 1.6.2”；PWA/iPhone 安装名称与设置页版本自动读取项目版本。升级只改项目及锁文件根版本，不替换依赖版本。
- “全部分类”改为猫咪图片列表，按收支分组；当月有旧账单或仍选中的停用分类显示于“历史分类”；切换收支时清除不匹配分类。
- 此前猫咪素材、首页背景及浮动按钮的视觉改动已在源码，发布状态未确认；需要改素材时再查 `design/` 对应来源文档。
- 此前版本与安装名称修改已通过构建及安装名称/子路径核对，未做全量回归或 iPhone 实测；本轮恢复首页最近记录，按低风险 UI 规则未运行测试、lint 或构建。
- 当前会话未提交、推送或部署，线上状态未查询；交接保持约 40～60 行，不累积历史过程。
