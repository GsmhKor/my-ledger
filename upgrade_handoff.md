# 我的账本：精简交接

最后更新：2026-09-05。新会话快速阅读本文，只按本轮需求查看相关代码。
本文固定使用小写文件名 `upgrade_handoff.md`，保持在 100～150 行以内。

## 1. 当前项目状态

- 项目：`my-ledger`，面向个人使用的 iPhone 记账 PWA。
- 当前版本：`1.4.0`，用户已授权本版提交推送；本文随版本提交更新，实际推送状态以 Git 为准，未核查 Pages 部署结果。
- 开发环境：Windows + VS Code；手机通过 Safari 添加到主屏幕使用。
- 仓库：<https://github.com/GsmhKor/my-ledger>，分支 `main`。
- 线上：<https://gsmhkor.github.io/my-ledger/>，部署子路径 `/my-ledger/`。
- 已有：首页月度结余、快速记账、账单编辑/删除/筛选、统计、备份与设置。
- 新增支出分类：餐饮、交通、日用品、金丝熊、其他；收入分类共 5 项，金丝熊改名纳入 `1.3.0`。
- 当前源码视觉：奶油猫咪主题、深色模式；原有 11 种图案（含 5 种分类、3 种导航），另有首页绣球花猫咪背景。
- 素材含 AI 改绘，不是原贴纸逐像素裁切；尺寸变体不算独立图案。
- 用户已同意将首页背景及局部修补纳入 `1.4.0`；当前无其他明确业务待办，第 4 节仅为已知问题。

统计口径（除非用户要求，不主动调整）：

- 最大支出：所选月份最高单日支出合计，包含已录入的未来日期支出。
- 当前月平均支出：截至今天的支出 ÷ 已过日历天数，包含零支出日，排除未来日期。
- 当前月预测：未舍入日均 × 本月总天数，最终显示整数日元。
- 历史月平均按整月计算，预测显示实际总额；未来月平均与预测显示「—」。
- 收入不参与；整月无支出时显示空状态。日期取设备本地日期，无跨午夜定时刷新。

## 2. 技术栈与目录职责

- React 19、TypeScript 6、Vite 8；精确依赖版本以锁文件为准。
- `idb` 管理 IndexedDB，`vite-plugin-pwa` 管理 PWA；oxlint 用于静态检查。
- sharp 仅用于开发时处理图片；无路由库、状态库、UI 框架或图表库。
- Tab 使用 React state；统计图采用 CSS 圆环和柱状图。

| 位置 | 职责 |
| --- | --- |
| `src/pages/`、`src/components/` | 页面、共享记账弹层、账单行和分类图标 |
| `src/hooks/`、`src/services/` | 状态、主题、账单存储与备份 |
| `src/constants/`、`src/types/`、`src/utils/` | 分类、数据类型、日期与金额工具 |
| `src/assets/`、`public/` | 内部插图及 PWA 图标 |
| `design/`、`scripts/` | 图案母版、来源说明及图片生成脚本 |
| `src/App.tsx`、样式文件 | 导航、共享月份、弹层与外观 |
| `vite.config.ts`、`.github/workflows/` | 子路径、PWA 和 Pages 部署配置 |

## 3. 长期产品与数据约束

- iPhone 优先，兼顾单手操作、安全区域、深色模式；装饰不妨碍记账。
- 纯本地，无账户、后端、云端同步；不上传账单，不接入分析、广告或追踪。
- 不主动增加银行连接、OCR、AI 分类、社交或 App Store 发布。
- 设备与浏览器环境各自存储账单，没有账户级隔离或跨设备同步。
- 实际记账仅支持 JPY：金额为正的安全整数，收支方向由 `type` 表示。
- CNY 仅为类型预留，不能视为已实现多币种记账。
- 保留已有账单、稳定分类 ID 和旧备份；不通过清库、改库名或删除存储解决升级问题。
- IndexedDB：`my-ledger-db`，版本 `1`，仓库 `transactions`，主键 `id`。
- JSON：`app: 'my-ledger'`、`schemaVersion: 1`，保留所有账单字段。
- JSON 恢复在同一事务中覆盖全部账单，不是合并；保留 CSV 导出和完整 JSON 备份/恢复。
- 账单日期为本地 `YYYY-MM-DD`；保留币种、备注及创建/更新时间等字段。
- 清空数据必须二次确认；不以卸载或清除网站数据作为刷新版本的常规方法。
- 停用分类使用 `retired: true`，仅隐藏新增选项；历史显示、筛选、统计及导出仍识别。
- 编辑旧账单时允许保留其原停用分类，不删除或重写历史分类 ID。
- 停用分类：超市、购物、房租、水电煤、通讯、娱乐、医疗、旅行。
- 保留部署子路径下的资源引用；主题偏好键为 `my-ledger-theme`。

## 4. 尚未完成或已知问题

以下是已有记录中的限制，不代表均已实机复现，也不是默认修复或验证清单。

1. Workbox 预缓存规则缺少 WebP，两张内部插图的离线缓存不完整。
2. 初始读取失败被显示为空列表，缺少错误提示和重试。
3. JSON 导入缺少真实日期、时间戳、空/重复 ID、分类与类型匹配的完整校验。
4. 导入接受 CNY，但编辑保存 JPY、汇总不区分币种，存在静默错算风险。
5. 数据库金额校验与编辑器不统一；累计金额没有安全整数溢出检查。
6. 删除、清空的异常反馈及恢复/清空/记账并发交互不完善。
7. PWA 自动更新缺少版本提示、恢复前台检查和未保存表单保护。
8. iPhone 键盘、弹层、安全区域、长金额、深色与离线重启尚无完整实机确认。
9. 弹层焦点与无障碍仍需完善；每日柱状图主要靠 title 提供明细。
10. CSV 公式字符、回车转义以及 Safari 下载/恢复仍有待按需检查。
11. maskable 图标安全区、持续跟随系统主题及图表配色仍有改进空间。

## 5. 本次版本登记与改动

- 首页原橙色金额卡片改为附件绣球花猫咪背景，使用 50% 不透明度，动态金额保持独立显示。
- 内置 image_gen 去除图片中的标题、金额、圆点及分隔线，保留花边和猫咪；属于 AI 编辑/改绘。
- 按用户圈图清理底部中央猫咪脸旁黑色小方块；放大局部用 image_gen 修补，仅将瑕疵区域合成回母版并重新生成应用 PNG。
- `HomePage.tsx` 加入独立背景层，原金额计算不变；`App.css` 仅让图片半透明，真实标题与金额保持不透明。
- 文字后使用 86% 不透明度的主题底色衬底；浅色深棕字、深色浅字。金额按字符长度及卡片宽度缩小，极长时换行而非省略。
- 卡片采用 3:2 首选比例、最小高度 240px，背景 cover 居中；窄屏可能裁去部分外围花边，未做手机实机验证。
- 新增 `design/generated/balance-hydrangea-master.png`、`src/assets/balance-hydrangea.png`（1200px 宽）；完整提示词见 `design/balance-hydrangea-source.md`。
- `scripts/generate-icons.mjs` 加入背景重建步骤；本轮仅缩放该图片，没有运行全量资源生成脚本。
- 用户要求 push 并改为 `1.4.0`；已统一 package.json、锁文件根项目和设置页版本，README 补充说明。
- 未运行 lint、build、测试或应用兼容性检查；未修改账单、分类 ID、存储、备份格式或第三方依赖。
- 本轮仅做提交推送所需的 Git 操作；main 推送会触发现有 Pages 工作流，不额外等待部署或核验手机更新。

## 6. 下一会话开发规则

- 快速阅读本文即可；不要求每次扫描整个项目或读取完整 Git 历史。
- 默认不读取历史归档（如 `docs/upgrade_history.md`）；仅在任务确需追溯时按需读取。
- 只修改用户明确要求的内容，不主动修复第 4 节问题或扩大范围。
- 仅查看直接相关代码；需要处理提交状态时才按需使用 Git。
- 低风险：文字、CSS、布局、图标、显示逻辑、小型 UI、简单条件判断，完成后直接说明改动。
- 上述低风险修改默认不运行任何测试、lint、build 或兼容性检查。
- 也不默认检查 CRUD、备份恢复、IndexedDB、Service Worker、PWA 或 GitHub Pages。
- 普通功能仅检查直接涉及的代码与逻辑，不默认全项目扫描或全量回归。
- 高风险范围：IndexedDB/`database.ts`、`backup.ts`、JSON/CSV 格式、schema/分类 ID、数据迁移。
- 高风险范围还包括 Service Worker/PWA、Vite/Pages/部署配置和依赖升级。
- 高风险也只验证受影响部分；普通图标或文案不因应用使用 PWA 而升级为高风险。
- 不为“保险”增加验证；仅用户明确说“完整验证”“发布前检查”“全面测试”才做较完整验证。
- push、更新版本号本身不等于完整验证授权；不因历史检查记录重复执行。
- 会话结束更新本文的当前状态、未发布改动和必要待办，替换过时内容，不累积历史流水账。
- 如实记录是否验证或发布，不为补写交接额外检查，不保存个人账单、备份内容或密钥。
